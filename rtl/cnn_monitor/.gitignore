# VCS, DC, waveforms, mapped netlists, reports generated by tools, and all
# temporary work libraries belong to one versioned run directory.  Source,
# small committed vectors, scripts, constraints, and curated reports remain
# outside this ignored subtree.
/runs/**
**/__pycache__/
**/*.pyc
