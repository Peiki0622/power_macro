"""Software-only CNN microarchitecture search helpers.

This package deliberately lives outside ``rtl/cnn_monitor``.  Stage 1 is a
contract and scheduling study; it must not change the legacy RTL or make the
search appear to be an RTL implementation.
"""

